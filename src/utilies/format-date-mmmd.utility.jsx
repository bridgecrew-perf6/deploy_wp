// export const FormatDateMMD = (dateString: string): string => {
//     const date = new Date(dateString);
//     return `${date.toLocaleDateString('en-us',{ month: 'short'})} ${date.getDate()}`;
// }